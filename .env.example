# --- Streamlit UI (local .env / Streamlit Cloud secrets) ---
GROQ_API_KEY=
MCP_SERVER_URL=https://<your-service>.up.railway.app/mcp
MCP_CLIENT_TOKEN=

# --- MCP server (Railway service variables) ---
# Same value used to mint MCP_CLIENT_TOKEN via `python -m app.make_token`.
MCP_SHARED_SECRET=
